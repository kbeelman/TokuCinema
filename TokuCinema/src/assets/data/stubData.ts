import {  } from '../../domain/Enums';
import { Movie } from '../../domain/Movie';
import { Media } from '../../domain/Media';

export const stubMovies = new Array<Movie>();
    

export const stubMedia = new Array<Media>();
